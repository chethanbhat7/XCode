npm run dev# MongoDB Connection
# For local development, make sure MongoDB is running on localhost:27017
# For remote MongoDB (e.g., MongoDB Atlas), use: mongodb+srv://username:password@cluster.mongodb.net/xcode_db?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=xcode_db
